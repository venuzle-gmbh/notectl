import type { UndoLocale } from '../UndoLocale.js';

const locale: UndoLocale = {
	label: 'Rückgängig machen',
	tooltip: (shortcut: string) => `Rückgängig machen (${shortcut})`,
};

export default locale;
