{
	"name": "notectl-monorepo",
	"private": true,
	"scripts": {
		"build": "turbo run build",
		"test": "turbo run test",
		"test:e2e": "playwright test",
		"test:showcase": "playwright test showcase-cut-paste --project=chromium",
		"test:showcase:angular": "CI=true playwright test angular/showcase-cut-paste --project=angular",
		"docs:screenshots": "playwright test --config=playwright.screenshots.config.ts",
		"demo:record": "playwright test --config=playwright.demo.config.ts",
		"markdown-demo:record": "playwright test --config=playwright.markdown-demo.config.ts",
		"preview:starlight": "pnpm --filter @venuzle/notectl build && pnpm --filter docs-site build && pnpm --filter docs-site preview",
		"lint": "biome check .",
		"lint:fix": "biome check --write .",
		"format": "biome format --write .",
		"typecheck": "turbo run typecheck",
		"size:check": "turbo run size:check",
		"dev": "turbo run dev"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.4",
		"@playwright/test": "^1.62.1",
		"turbo": "^2.10.8",
		"typescript": "^5.9.3"
	},
	"packageManager": "pnpm@9.15.0"
}
